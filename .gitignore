node_modules
dist
dist-ssr
*.local
.DS_Store

# apps/hr is its own git repository (github.com/ccentrikhrapp/onboardingapp),
# not part of this monorepo — kept on disk here only for local dev convenience.
apps/hr/

# Secrets / local env — never commit real keys
.env
.env.*
!.env.example

# Supabase CLI local state (each app has its own supabase/ dir)
**/supabase/.branches
**/supabase/.temp
**/supabase/.env

.vscode/*
!.vscode/extensions.json
.claude/
**/scripts/.tmp
_flow_entry.jsx
_smoke_entry.jsx
_h.jsx
_*.mjs
*.output
npm-debug.log*
